export type ClassType = {
    id: number;
    title: string;
    description: string;
    image: string;
  };
  
  export type FeatureType = {
    id: number;
    title: string;
    description: string;
    icon: string;
  };